// Partners & Compliance — reuses the existing PartnerAccreditationReportingPanel.
// It covers partner reporting, accreditation bodies, verification, and submission history.
import PartnerAccreditationReportingPanel from "@/components/admin/stage4/PartnerAccreditationReportingPanel";

export default function SMSPartnersPanel() {
  return <PartnerAccreditationReportingPanel />;
}
